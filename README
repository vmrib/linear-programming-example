==> Estrutura do projeto
- doc: documentos sobre modelo e implementação
- source: código fonte
- include: arquivos cabeçalho
- lib: bibliotecas e códigos fonte das bibliotecas
- build: arquivos objetos
- example.txt: exemplo de entrada
- despacho: executável

==> Compilação e execução
Além do como "normal" de funcionamento, o programa pode ser compilado no modo
"resultado". Nesse modo, além de escrever a saída do modelo em stdout, o
programa também resolve e exibe o resultado do PL, incluindo comentários sobre
a resolução, no seguinte formato:

<modelo, no formato de entrada do lpsolve>
<comentários sobre a resolução>
Results
<resultados>

Para compilar o programa
$ make

Para rodar o programa com o exemplo
$ make run

Para compilar o programa no modo "resultado"
$ make clean
$ make result

Para compilar no modo debug
$ make clean
$ make debug

Para remover objetos e execultável gerados
$ make clean

Para remover objetos, execultável e .a da biblioteca
$ make purge